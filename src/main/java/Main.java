import javax.persistence.*;

public class Main {

    public static void main(String[] args) {
        Login newLogin = new Login();
        newLogin.setSize(400,130);  //set size of the frame
        newLogin.setVisible(true);  //make form visible to the user
    }
}